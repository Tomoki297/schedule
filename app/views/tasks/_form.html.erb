<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this task from being saved:</h2>
      <ul>
        <% task.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <div class = "label"><%= form.label :task, "予定" %>
      <%= form.text_field :task %>
      <br>
      <%= form.label :limit_date, "予定締め切り" %>
      <%= form.datetime_select :limit_date %>
      <br>
      <%= form.submit %>
    </div>
  </div>
<% end %>
